/**
 * @format
 * @flow
 */

export const changePassword = () => {
  return new Promise(resolve => {
    resolve('OK');
  });
};

export const logout = () => {
  return new Promise(resolve => {
    resolve('OK');
  });
};
