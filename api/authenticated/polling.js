export const fetchPollings = () => {
  return new Promise((resolve) => {
    resolve('OK');
  });
};

export const votePollings = () => {
  return new Promise((resolve) => {
    resolve('OK');
  });
};
