import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  centerBox: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  },
  fullWidth: {
    width: '100%'
  }
});
